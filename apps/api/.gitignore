bin/
tmp/
*.exe
*.test
*.out
.air.tmp
.env
coverage.out
# Generated code is committed (both sqlc and oapi-codegen output). Run
# `make generate` (which calls sqlc generate + oapi-codegen) after editing
# internal/db/queries/ or openapi.yaml and commit the diff. CI does not run
# either generator, so the committed files are the source of truth that
# `go mod tidy` consults to keep direct deps anchored.
coverage.txt
coverage.out
