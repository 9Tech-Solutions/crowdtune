/** A single image asset with pixel dimensions. */
export type Image = {
  /** URL of the image resource. */
  url: string
  /** Pixel width (positive integer). */
  width: number
  /** Pixel height (positive integer). */
  height: number
}
