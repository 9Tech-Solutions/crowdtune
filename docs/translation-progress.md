# Translation Progress

Tracks every Festify file we plan to port. Add entries as we pick targets. Update as items move through the
clean-room workflow described in `docs/translation-workflow.md`.

## Legend

- `[ ]` queued
- `[~]` translator-spec drafted, in review
- `[s]` spec approved, awaiting implementation
- `[i]` implementation in progress
- `[x]` shipped (commit hash linked)

## Frontend components (`.reference/festify/src/`)

- `[i]` `state.ts` - root domain types. Spec at `docs/specs/state.spec.md` passed firewall review. Implemented so far: `Party`, `PartySettings`, `Playback` (entities/party); `Track`, `TrackReference`, `Metadata` (entities/track); `Playlist`, `PlaylistReference` (entities/playlist); `Image` (shared/model, lifted out of party to avoid same-layer cross-slice import). Deferred: `ConnectionState`, view-layer state shapes, `User` slice (waits on Neon Auth shape), `Player` slice (waits on Phase 9 Spotify SDK), Zustand stores. Lock-now decisions applied: ISO 8601 timestamp strings on the wire, `spotifyUserId` rename, `provider: 'spotify'` literal union, camelCase fields throughout. Typecheck green. Commits: 6b15b11 (HeroUI directive), 0c7b728 (entity types).
- `[i]` `selectors/track.ts` - track-related selectors. Spec at `docs/specs/selectors-track.spec.md` passed firewall review. Implemented 9 pure functions in `apps/web/src/entities/track/lib/` (identity.ts, queue.ts, labels.ts, load-candidates.ts) with 67 Vitest tests covering every section-5 edge case. Lock-now decisions applied: sort by `Track.order` ascending with NaN guard treated as +Infinity; tie-break by `addedAt` lex string compare; strict `===` equality with `== null` idiom only for null/undefined collapse; duration unit `minutes * 60_000` ms; vote-status display strings are CrowdTune-original copy (`'Now playing'` / `'Paused'` / `${n} votes` / `'1 vote'` / `'Host pick'` / `'Pending'`), not Festify verbatim. State-reading accessors (spec sections 2b-2e) intentionally skipped - we have no Redux store; consumers pass data into pure functions directly. Spec-fidelity review by typescript-reviewer: all 9 functions matched, no firewall breach, FSD downward-only flow verified (`labels.ts` imports `Playback` from `@/entities/party` as a permitted same-layer exception). Typecheck exit 0; 68 tests pass (67 new + 1 pre-existing HomePage). Commit: 2d3a258.
- `[i]` `selectors/party.ts` - party-related selectors. Spec at `docs/specs/selectors-party.spec.md` passed firewall review. Implemented 5 pure functions in `apps/web/src/entities/party/lib/party-selectors.ts` (`isHost`, `playbackMasterId`, `isPlaybackMaster`, `hasOtherPlaybackMaster`, `playbackState`) with 37 Vitest tests covering every section-5 edge case. Lock-now decisions applied: strict `===` host check via JWT `sub` claim vs `party.createdBy`; plain `boolean` return (no tri-state); empty-string master id treated as "no master designated" (same as null); `localInstanceId` accepted as `string | null | undefined` with empty-string normalization. Spec function 2b (party identifier accessor) dropped - TanStack Router's `useParams` provides this in components. Spec-fidelity review by typescript-reviewer: all 5 functions matched, no firewall breach, FSD intra-slice imports only. Typecheck exit 0; 105 tests pass (37 new). Note: `isPlaybackMaster` / `hasOtherPlaybackMaster` accept `localInstanceId` as a parameter; the player-instance-id Zustand slice that supplies it is a separate future port (deferred per state.ts open question).
- `[i]` `selectors/playlists.ts` - playlist-related selectors. Spec at `docs/specs/selectors-playlists.spec.md` passed firewall review (zero code fences). Implemented 1 pure function `filterPlaylists(playlists, query)` in `apps/web/src/entities/playlist/lib/filter-playlists.ts` with 20 Vitest tests covering every section-5 edge case (empty inputs, short-circuit no-allocation when query is empty, case-insensitive substring match, multiple matches preserve input order, regex metacharacters treated literally, no whitespace trim, empty-name playlists). Spec sections 2a + 2b (raw state accessors) intentionally skipped - we have no Redux store; consumers pass data into the pure function directly. Lock-now decisions: no sort applied (caller's choice), no whitespace trim (parity with reference), no debouncing (call-site concern). Typecheck exit 0; 125 tests pass (20 new + 105 pre-existing).
- `[i]` `views/party-track.ts` - first UI port (SHIPPED). Implemented at `apps/web/src/entities/track/ui/PartyTrackRow.tsx` as a fully-controlled function component (20 props: 16 derived data + 4 callbacks). Spec at `docs/specs/views-party-track.spec.md` passed firewall review with 4 fixes applied (dropped one Festify field-name parenthetical, locked skip-disabled-during-toggling default, converted 2 implementer-answerable questions to lock-now decisions). HeroUI primitives used: Button (isIconOnly), Avatar with Avatar.Image / Avatar.Fallback compound, Spinner. 8 inline SVG icons since HeroUI v3 ships no icon set. 34 new Vitest tests (31 from implementer + 3 reviewer-driven adds for null-track-record skip-hidden + standard-host-scenario play-button-enabled). Typecheck exit 0; 159 tests pass. **Tooling note**: heroui-react MCP server's tools were unavailable to the subagent on this port; implementer substituted direct `node_modules/.pnpm/@heroui+react@3.0.4*/dist/components/<name>/index.d.ts` inspection. Fix landed afterwards (commit `62ad2cb`, agent frontmatter `mcpServers: [heroui-react]` + settings.local.json explicit allow-list); future UI ports use MCP directly. Open questions deferred to follow-up ports: cover-image primitive choice, anonymous-voting product knob, playing-row visual treatment.
- `[i]` `views/queue-drawer.ts` - second UI port (SHIPPED). DISCOVERY: this is the **party navigation sidebar** (brand mark, nav menu with active-link highlighting, user identity row, account menu, animated menu swap), NOT a track-list drawer. Implemented at `apps/web/src/widgets/queue-drawer/ui/QueueDrawer.tsx` as a fully-controlled widget (15 props; spec at `docs/specs/views-queue-drawer.spec.md`). Lock-now overrides applied: full-height left sidebar on all breakpoints, Legal/Privacy links omitted (URLs unknown), brand mark is a text wordmark "CrowdTune" pending the SVG asset. HeroUI primitives via MCP: `Drawer` compound (Backdrop/Content/Dialog/Header/Body with `placement="left"` + `isDismissable` + Escape via React Aria), `Text` for wordmark and username. 30 new tests. Reviewer caught one HIGH: nav links carried `href="#"`; fixed to pass real route paths so right-click/middle-click anchor semantics work.
- `[i]` `views/party-queue.ts` - third UI port (SHIPPED). The actual in-party track-list view; composes `PartyTrackRow` one per track. Implemented at `apps/web/src/widgets/party-queue/ui/PartyQueue.tsx` as a fully-controlled widget; spec at `docs/specs/views-party-queue.spec.md`. Three mutually exclusive render states (loading -> HeroUI Spinner; empty role-differentiated -> host gets a settings link, guest gets info text; populated -> list of PartyTrackRow). Currently-playing row is at index 0 (no sticky header / no separate now-playing slot). Reorder animation as progressive enhancement using the View Transitions API, with a `prefers-reduced-motion: reduce` guard that skips the transition entirely. Locked decisions: server-confirmed votes (no optimistic toggle), vote/remove/togglePlayback/transferPlayback callbacks forwarded verbatim to each row, queue `onRemove` wired to both row `onRemove` and row `onSkip` (the queue is action-agnostic; feature layer can split later). 19 new tests. PartyTrackRow extended with two new optional props `hasPlayingRowAbove?` (extra top padding on index 1 to create the visual gap below the playing row) and `isEvenRow?` (alternating stripe via the HeroUI `bg-default` token; the playing row at index 0 never receives the stripe).
- `[i]` `views/view-party.ts` - fourth UI port (SHIPPED). The page shell that composes the QueueDrawer + a fixed header (party name + playback-progress-bar slot) + the sub-view slot (PartyQueue on the queue route; `Outlet` for sub-routes) + a sign-in modal. Implemented at `apps/web/src/pages/party/ui/PartyPage.tsx` with a thin TanStack Router route at `src/routes/party.$partyId.tsx`. Two co-located hooks `usePartyQuery(partyId)` and `usePlaybackQuery(partyId)` are placeholder seams (queryFn returns null) - when the party + playback backend lands, only the queryFn changes. **Auth-client refactor (incidental):** the page port surfaced that `@/shared/auth/auth-client.ts` re-exported the Neon Auth client without typed wrappers. Added `getSession()` and `signInWithSocial(provider, callbackURL)` typed helpers in that module so call sites avoid the `unknown` cast that the reviewer caught was actually broken at runtime (`getSession` lives on `authClient.adapter`, not at the top level). **Test infrastructure:** new `apps/web/src/shared/test/test-providers.tsx` exports `TestQueryProvider` for mocking TanStack Query in unit tests. HeroUI primitives via MCP: `Modal` compound (Backdrop / Container / Dialog / Header / Heading / Body / Footer), `Spinner`, `Button`, `Text`. OAuth provider list locked to `['google']` (Spotify excluded explicitly because it is wired via Phase 9 Spotify endpoints, not Neon Auth social sign-in - verify the actual enabled-providers set with product before adding more). 15 new tests; 223 total (one inert follow-up-mode test was dropped because there is no in-page seam to force follow-up mode without a real auth client integration; the behavioural contract remains in the spec). Reviewer-driven fixes applied: dropped the dangling `SignInMode` barrel re-export; replaced `unknown` casts with typed auth-client wrappers; rewrote the keydown handler to use effect deps instead of refs (fixed `react-hooks/refs` ESLint error); added `isDisabled` wiring on provider buttons; dropped a stale `eslint-disable` directive in QueueDrawer. **Follow-up RESOLVED in subsequent commit**: the wide-viewport drawer architecture issue (HeroUI Drawer focus trap on wide screens) is fixed. `QueueNav` was extracted from `QueueDrawer` as a chrome-less navigation component; `QueueDrawer` now wraps `QueueNav` in the HeroUI Drawer modal chrome for narrow viewports, while PartyPage renders `<QueueNav>` directly inside a plain `<aside>` for wide viewports. No focus trap, no backdrop intercepting pointer events. Public API of `QueueDrawer` is unchanged; new `QueueNav` + `QueueNavProps` are exported from the widget barrel for callers that want the chrome-less variant.
- `[i]` `views/party-settings.ts` - eighth UI port (SHIPPED). The host-only settings sub-view that mounts at `/party/$partyId/settings`. Spec at `docs/specs/views-party-settings.spec.md`. Implementer agent was stopped mid-run (see handover note in prior session); resumed by hand-finishing the partial files rather than wiping and re-dispatching. The widget composes two side-by-side panels (General Settings + Fallback Playlist) inside a host-gated shell at `apps/web/src/widgets/party-settings/ui/PartySettings.tsx`; subcomponents `GeneralSettingsPanel.tsx` and `PlaylistPanel.tsx` are colocated. Thin TanStack Router route at `src/routes/party.$partyId.settings.tsx`. **Five placeholder hooks** under `widgets/party-settings/api/`: `useUpdatePartySettings(partyId)`, `useUpdatePartyName(partyId)` (separate because party name is a Party field, not a PartySettings field), `useFlushQueue(partyId)`, `useInsertPlaylist(partyId)`, `useHostPlaylists(partyId, isSpotifyConnected)`. Each carries a docblock explaining what the real implementation will do (call `api()` from `@/shared/api`, invalidate the relevant query key). `DEFAULT_SETTINGS` constant lives at `widgets/party-settings/model/defaults.ts` (separated from the component file to satisfy `react-refresh/only-export-components`). Auto-save: toggles fire `onChange`; text/number inputs fire on blur-when-changed. Inverted polarity preserved on the "Require sign-in to vote" toggle (visual ON = `allowAnonymousVoting: false`); the other two toggles use positive labels so polarity is straight. Flush queue uses a HeroUI Modal confirmation with `isDismissable={false}` so accidental backdrop clicks don't dismiss the destructive prompt; Escape still works (separate prop). Host-gate spinner waits for BOTH the party record AND the session query to resolve, preventing an unauthorized flash while the session is in flight. **Lock-now decisions applied**: widget (not page) FSD placement; `partyId` read via `useParams({ from: '/party/$partyId' })` inside the widget; no route props; placeholder hooks resolve immediately with `mutationKey`/`queryKey` parameterized by `partyId`; party name updates fire `useUpdatePartyName` (a distinct hook from `useUpdatePartySettings`) so the placeholder contract is honest about what's being saved; `console.warn` is the interim fallback for failure feedback (matches `party-track-search` pattern). **HeroUI primitives via MCP**: Switch (compound: `.Control` / `.Thumb` / `.Content`; uses `data-selected` and native `checked` rather than `aria-checked` - tests use `.toBeChecked()`), Input (`fullWidth`), Button (`variant="danger"` for Flush), Modal (compound: `.Backdrop` / `.Container` / `.Dialog` / `.Header` / `.Heading` / `.Body` / `.Footer`), Spinner, Label, Description. Switch `onChange: (isSelected: boolean) => void` confirmed via MCP (NOT `onValueChange`). 37 new tests; 332 total. Reviewer-driven test changes: 3 polarity assertions corrected from `aria-checked` to `toBeChecked`, 1 retargeted from `mockMutateUpdate` to `mockMutateUpdateName` (party-name flow now has its own mutation), and 2 new tests added covering "name change fires update-party-name mutation" + "empty name blur does not fire mutation (validation error)". **Reviewer-driven fixes applied before commit**: H1 added session-loading gate to spinner (was flashing unauthorized while session resolved); M2 added `useUpdatePartyName` placeholder hook so party name actually fires a mutation (was logging `console.warn` only); M3 corrected `aria-busy` to string serialization (`'true'`/`'false'`); M4 added `isDismissable={false}` on the flush Modal.Backdrop; M1 amended the polarity comment block to explain the per-toggle relabeling. Deferred concerns: real backend wiring for all 5 mutation/query hooks (Phase 8+); Spotify OAuth integration (Phase 9c - `handleConnectSpotify` is a placeholder); cover image extension on the `Playlist` type (separate translation task if product requests it); toast notification primitive (project-wide wiring task); silent-refresh-on-401 logic for Spotify token expiry; settings-saved live-region announcement (div is rendered but unused - infrastructure for a future polish pass). Open product questions in spec section 6: confirmation copy for flush dialog, queue-length observable from the settings view, dangerous-action scope (no End/Delete Party action shipped).
- `[i]` `views/party-track-search.ts` - seventh UI port (SHIPPED). The search/add-track sub-view that mounts in PartyPage's `<Outlet />` at `/party/$partyId/search`. Implemented at `apps/web/src/widgets/party-track-search/ui/PartyTrackSearch.tsx` with a NEW companion component `SearchResultRow.tsx` (the source re-uses its queue-row template for results; CrowdTune deliberately splits these into separate components with simpler contracts). Two placeholder hooks live at `widgets/party-track-search/api/`: `useSearchTracks(partyId, query)` and `useAddTrack(partyId)`, both with docblocks explaining the deferred backend wiring. Thin TanStack Router route file at `routes/party.$partyId.search.tsx`. Dual-tracked search state: local `useState` for keystroke-tight input + URL search param via `useSearch`/`useNavigate` for debounced committed query (300ms locked). Enter cancels debounce + fires immediately; Escape clears input + URL param + results. Lock-now decisions: clear-and-show-loading on re-fetch (no stale overlay), already-queued rows STAY in results with the existing vote count + ghost variant button (NOT removed), idle state shows the proposed prompt (no recommendations section), result count cap 20, sign-in gate deferred to feature layer (add button is enabled regardless of auth; placeholder mutation is a no-op). HeroUI primitives via MCP: Input, Avatar (compound), Button (variant=outline / ghost), Spinner. Accessibility: `aria-label` on input, `aria-live="polite"` widget-level region announcing add success, `aria-busy` on results region while loading, per-button `aria-label` "Add [title] by [artist] to queue" / "Adding [title]..." swap during pending. 35 new tests; 295 total. **Reviewer-driven fixes applied before commit**: dropped a dead `results.map` branch in `renderContent` that was unreachable behind the `hasResults` guard at the call site; removed contradictory `onPress` handler on the already-queued `isDisabled` button.
- `[i]` `views/playback-progress-bar.ts` - sixth UI port (SHIPPED). The thin 1px progress strip that anchors to the bottom of PartyPage's fixed header. Spec at `docs/specs/views-playback-progress-bar.spec.md`. Implemented at `apps/web/src/widgets/playback-progress-bar/ui/PlaybackProgressBar.tsx` as a fully-controlled, two-prop widget (`playback: Playback | null`, `durationMs: number | null`). Source's clever two-frame `requestAnimationFrame` + CSS-transition trick preserved: frame 1 anchors at the current computed position with zero transition; frame 2 starts a long `linear` transition over the remaining duration, so the browser compositor drives the fill animation without JS ticks mid-track. Cleanup cancels both frame handles on unmount or before each new effect cycle. **HeroUI ProgressBar was rejected** via MCP query: it controls its fill through `value`-prop reconciliation with no escape hatch to write `style.transition` per-frame on the DOM node. Hand-rolled `<div>` with Tailwind + inline `style.transform = 'scaleX(...)'` on the fill is the correct path; this is the first port where the MCP query led to "hand-rolled" rather than "use HeroUI primitive." ARIA: `role="progressbar"`, `aria-valuemin=0`, `aria-valuemax=100`, `aria-valuenow` updated via `setInterval(..., 1000)` writing through `setAttribute` to avoid React reconciliation overhead. `aria-valuenow` omitted when data is absent. 20 new tests; 260 total. **PartyPage integration deferred to a separate commit** (per the locked decision in this port's brief) so the placeholder-to-widget swap is auditable on its own. Two open product questions deferred: respect for `prefers-reduced-motion` on the slow linear fill (currently not respected; 1px strip considered below the threshold of concern), and PartyPage header layout's 1px height reservation when data is absent.
- `[i]` `views/party-share.ts` - ninth UI port (SHIPPED on 2026-05-12). The host-only share sub-view that mounts at `/party/$partyId/share`. Spec at `docs/specs/views-party-share.spec.md`. Implemented at `apps/web/src/widgets/party-share/ui/PartyShare.tsx` with a colocated `useWebShare` hook at `model/use-web-share.ts` and a thin TanStack Router route at `src/routes/party.$partyId.share.tsx`. The widget reads `partyId` via `useParams({ from: '/party/$partyId' })` and binds NO route props. Two cache reads: `['party', partyId]` (warmed by the party page shell) and `['session']` (auth identity). Loading spinner waits for BOTH to resolve before the host-gate check runs, matching the party-settings H1 lesson. Host-gate is defensive in-widget; non-hosts see an unauthorized message in-place (no redirect). Widget renders a description paragraph (with the application origin underlined and nowrap-clipped), the party's short join code in large centered selectable text, and a Share button that appears only when `navigator.share` is callable. **NO backend hooks** - this widget makes zero network calls; the Web Share API is a browser API. `useWebShare` returns `{ isSupported, share(payload) }`; the `share()` call is wrapped in try/catch and rejection is swallowed silently with `console.warn` (spec OQ-4 - no toast). All CrowdTune-original copy is annotated with "product copy review" comments at use sites: description paragraph, share body text ("Join [name] on CrowdTune and help choose the music!"), button label, unauthorized message, loading label, join code aria-label. **Lock-now decisions applied**: `Party.shortId` confirmed as the join code field (spec OQ-5); URL `partyId` and `party.shortId` are the same value in CrowdTune so both display and share URL use `partyId` (spec OQ-6); `window.location.origin` accessed at render time, not module scope (SSR-safe pattern); Spinner carries `aria-hidden="true"` because the parent `role="status"` div is the labelled landmark. **HeroUI primitives via MCP**: Button (`variant="primary"` default), Spinner (`size="lg"`). 20 new tests; 352 total. Reviewer-driven fixes applied before commit: M2 dropped `!` non-null assertions on `party.name` by extracting `partyName` to a local before the closure (TS narrows `party` to `Party` after the early-return guard at the statement level, but the closure boundary re-widens it back to `Party | null` - the reviewer's claim that the `!` were redundant was wrong; the extracted-local fix is cleaner than reinstating `!`); M6 moved the `navigator.share` restore from inline-in-test to `afterEach` to prevent cross-test contamination if a test throws mid-flight; L8 added a test for the `isSessionLoading === true` branch (party present, session pending); L9 removed duplicate `aria-label` from the Spinner inside the labelled status region (double-announcement risk), replacing it with `aria-hidden="true"` since the parent `role="status"` div carries the accessible name.
- `[x]` `views/party-search.ts` - COVERED BY EXISTING PORT (no separate widget). Spec at `docs/specs/views-party-search.spec.md` documents the file as the parent results-container shell that Festify split out as its own custom element (loading/error/results state switch + per-row render). The existing CrowdTune `PartyTrackSearch` widget (commit `ef69bdb`) already collapses both Festify files (`views/party-search.ts` parent + `views/party-track-search.ts` row template) into a single widget. The split was a Polymer-specific concern that does not map meaningfully to React. Every behavior in this file maps onto code already shipped in `PartyTrackSearch`. No further implementation required; resolved by the prior port. Note: CrowdTune's idle and no-results states render distinct messages (Festify rendered blank), and the error state is a single paragraph (Festify used emoji + heading + subheading) - both are CrowdTune-original improvements and should be kept.
- `[s]` `views/search-bar.ts` - SPEC APPROVED, deferred enhancement to `PartyTrackSearch`. Spec at `docs/specs/views-search-bar.spec.md` documents the file as a narrow presentational input bar with one functional gap relative to the shipped `PartyTrackSearch` widget: a leading icon zone that displays a brand mark when the input is empty and a tappable back-arrow button when text is present (clears the input + URL param on tap). Every other behavior in the file (text capture, debounce, URL sync, initial value, Enter/Escape, auto-focus) is already covered. Recommendation (b) from the spec: do NOT create a new widget; instead enhance `PartyTrackSearch` by adding leading content to its HeroUI `Input` (either custom `startContent` or the `isClearable` prop). The enhancement is pending a product decision (open question 1 in the spec): is the icon swap actually wanted, and if so, what asset goes in the brand-mark slot? Until product resolves, no code change is needed.
- `[i]` `views/view-home.ts` - fifth UI port (SHIPPED). The public landing page (party joining + creation). REPLACES the prior Phase 0 skeleton at `apps/web/src/pages/home/ui/HomePage.tsx` (same `/` route). Spec at `docs/specs/views-view-home.spec.md`. Public to everyone, authenticated or not - join form is always visible; lower button has 4 mutually exclusive states (creating-in-progress, authenticated-and-Premium, auth-unknown/checking, not-authenticated). Browser-incompatible viewers see only the join form (no lower button). HeroUI primitives via MCP: Card / Card.Content (main panel), Input (party code field with `inputMode="numeric"`), Button (action variants with isDisabled), Text (wordmark + functional description), Spinner (inline loading on Joining... and Creating... labels). Functional description copy: "Let your crowd shape the soundtrack, one vote at a time." (CrowdTune-original). Spec invariant enforced: only one of join / create can be in flight at a time (handler guards + visual disable on the input + Join button when create is pending). 17 new tests; 240 total. Locked decisions: useJoinParty / useCreateParty are placeholder mutations resolving with `{ partyId: <code> }` and `{ partyId: 'placeholder' }` respectively (no backend yet); useSpotifyStatusPlaceholder returns `{ isConnected: false, isPremium: false }`; usePlaybackCompatible returns false. Auth state is wired via TanStack Query against `getSession()` from `@/shared/auth` (the typed wrapper from the PartyPage port) - on mount the query is pending so the lower button shows "Checking..." until the session resolves, then the appropriate variant renders. Reviewer-driven fixes applied before commit: replaced hardcoded `authState='unauthenticated'` with the real session query; added the join/create cross-guard. Three open product questions in spec section 6 (party code format/length validation, QR scanning, recently-joined parties list).
- `[i]` `views/tv-track.ts` - tenth UI port (SHIPPED at `255127b` on 2026-05-12). Standalone presentational tile at `entities/track/ui/TvTrackCard.tsx`, NOT a variant on PartyTrackRow (lock-now #1: layout direction, cover size, vote display, and zero-interactivity surface diverge too far). Fully controlled with the 3-prop contract `{ trackRef, track, metadata }` (cross-spec reconciliation: view-tv §8 won over tv-track §2's single-prop-with-derivation proposal, matching every prior CrowdTune UI port). All 10 lock-now decisions applied. All 6 open questions resolved at the orchestrator level before implementer dispatch (fixed `w-52` tile width, `200` cover target, static Tailwind only, `'Loading...'` text, no `aria-hidden`, no vote count animation). HeroUI MCP: Avatar was queried but rejected because `Avatar.Image` defers rendering until the image loads which fails in jsdom; spec section 7 explicitly permits a plain `<img>` fallback. 17 new tests (16 spec section 12 + 1 reviewer-driven for the style passthrough prop). Pre-implementation refactor shipped at `05fe087`: extracted `pickCoverUrl(images, targetPx)` to `entities/track/lib/cover.ts` and deduped the two byte-identical copies at `entities/track/ui/PartyTrackRow.tsx:76` and `widgets/party-track-search/ui/SearchResultRow.tsx:30`. Both call sites continue to pass `54`; TvTrackCard passes `200`. Scope-locked typescript-reviewer cleared 16/16 checkpoints with no findings on the first review. **Style-prop passthrough added by reviewer-driven fix to TvPage**: TvTrackCard now accepts an optional `style?: CSSProperties` applied to the root `<li>` so the parent strip can attach `viewTransitionName` without inserting a wrapper element that would break `<ul>` HTML validity + ARIA list semantics. The prop is generic but the immediate motivation is the TvPage flip animation.
- `[x]` `views/app-shell.ts` - COVERED BY EXISTING FSD `app/` LAYER (no new component required). Spec at `docs/specs/views-app-shell.spec.md` (firewall: zero code fences). Festify's app-shell has exactly four responsibilities; the section 8 comparison table resolves them against CrowdTune's stack: (1) **routing dispatch** -> COVERED by TanStack Router file-based routes + `routes/__root.tsx`'s `<Outlet />`; (2) **dark background + viewport sizing** -> COVERED by `<html class="dark">` in `index.html` plus `@import "@heroui/styles"` in `app/styles/index.css`. Per-page wrappers handle their own viewport sizing (HomePage / PartyPage use `min-h-screen flex flex-col`; PartyTvPage uses `fixed inset-0` for the full-viewport overlay). Forcing `min-h-screen` at the root layout would conflict with TvPage; the page-owns-viewport pattern is correct as-is. OQ-1 resolved: no change needed; (3) **theme/provider mounting** -> COVERED by `NeonAuthProvider` in `app/providers/` (Redux/Polymer/fit-html are replaced by Zustand + TanStack Query + React function components; no equivalent mounting needed); (4) **default-to-home fallback** -> N/A (Festify's "default to Home when route absent" was a Polymer-router quirk; in TanStack Router the empty URL `/` IS the home route `routes/index.tsx`. Genuine 404 handling can be added via `notFoundComponent` later if needed, not part of this resolution). **Two genuine gaps identified but explicitly NOT part of an app-shell port**: (a) global toast / notification host is a future `features/notifications/` port (spec lock-now #3 - one visible toast at a time, no queue rendered, indefinite duration, store-driven open/close; HeroUI Toast is the preferred primitive subject to MCP verification of imperative open + duration control); (b) Spotify Web Playback SDK injection is a future playback feature port (spec lock-now #4 - belongs to the playback widget's lifecycle, not the root shell). No further implementation required for app-shell itself.
- `[x]` `selectors/users.ts` - PARTIAL coverage; doc-only with one orchestrator-level CORRECTION to the spec. Spec at `docs/specs/selectors-users.spec.md` (firewall: zero code fences). Two Festify selectors: (1) **user display name** -> COVERED, use `session?.name ?? session?.email ?? 'Guest'` inline at call sites; do not introduce a shared selector. (2) **Spotify account connected** -> **NOT COVERED** (the spec's section 4 + section 5 claim "active Neon Auth session implies Spotify connected" is FACTUALLY WRONG for our architecture: `NeonAuthProvider` only configures `social: ['google']`; Spotify is wired separately through `POST /api/spotify/token` writing to the `spotify_credentials` table). The correct lock: `useSpotifyStatusPlaceholder()` (shipped inline in HomePage during view-home port) is the current placeholder returning `{ isConnected: false, isPremium: false }`. The real implementation is a future `useSpotifyStatus(userId)` hook backed by a `GET /api/spotify/status` endpoint that reads `spotify_credentials` for the JWT `sub`. This belongs to the future Spotify-side frontend wedge (along with the SPA Connect Spotify flow + `GetValidAccessToken` helper), not a `selectors/users.ts` port. No new selector function required at this layer.
- `[x]` `selectors/routes.ts` - COVERED by TanStack Router with one orchestrator-level CORRECTION to the spec. Spec at `docs/specs/selectors-routes.spec.md` (firewall: zero code fences). Five Festify selectors all map to TanStack Router primitives: queue / search / settings / share / TV. Locked: no shared route-selector utility; callers use `useNavigate({ to: '/party/$partyId/<sub>', params })` or `<Link>` inline. The router handles URL encoding. The search `?s=` query key is correct as the spec proposes (consistent with the existing PartyTrackSearch URL state). **CORRECTION to spec section 3e / 4 / 5**: the spec proposes the TV route at `/tv/$partyId` and marks it PARTIAL because `routes/tv.$partyId.tsx` does not exist. That URL shape is WRONG for our architecture - the TV route is at `/party/$partyId/tv` (nested under the party route via TanStack file-based routing), shipped at `c893c6e` as `routes/party.$partyId.tv.tsx`. PartyPage short-circuits to `<Outlet />` on the tv subroute so the page fills the viewport without party-shell chrome. The TV route is therefore fully COVERED, just at a different URL than the spec assumes. The OQ-1 in the spec ("Is TV view in scope?") is resolved: shipped. The OQ-2 ("Is `?s=` consistent?") is resolved: yes, matches existing PartyTrackSearch.
- `[x]` `selectors/domain.ts` - COVERED by inline `window.location.origin` (no new utility required). Spec at `docs/specs/selectors-domain.spec.md` (firewall: zero code fences). Festify's single host-normalization selector prepends `www.` to bare production hosts and passes localhost / already-prefixed hosts through unchanged. Locked rejections: (1) the `www.` normalization is a Festify vanity convention NOT adopted by CrowdTune (our share URLs always use the full `window.location.origin` including protocol; no `www.` prefix logic anywhere). (2) No `shared/lib/joinUrl` extraction at this time - PartyShare's inline pattern (`window.location.origin + "/" + party.shortId`) is the only consumer; extract later if a second consumer emerges. (3) `VITE_PUBLIC_ORIGIN` env var is a forward-looking SSR concern (OQ-1) not blocking on this port; defer until SSR is on the roadmap. No new code shipped.
- `[i]` `views/view-tv.ts` - eleventh UI port (SHIPPED at `c893c6e` on 2026-05-12). Standalone full-viewport TV display page at `pages/party-tv/ui/PartyTvPage.tsx`, mounted at `/party/$partyId/tv` outside the party shell. Companion change to PartyPage: short-circuit branch returns `<Outlet />` when `currentSubView === 'tv'` so the QueueDrawer + header + sign-in modal chrome are bypassed. Thin TanStack Router route file at `routes/party.$partyId.tv.tsx`. Two-region layout: upper region with 49vh cover + metadata block (track name, artist, PlaybackProgressBar, `party.settings?.tvDisplayText`, join-code pill) + lower region with up to 29 TvTrackCards (`tracks.slice(1, 30)`). Background uses Ken Burns CSS keyframe (pure CSS, no library) on a deterministically-indexed background image (`trackName.length % backgroundImages.length`); falls back to cover-art-as-background when `metadata.backgroundImages` is empty. Both animations (Ken Burns + flip via View Transitions API) guarded by `prefers-reduced-motion`. Mouse cursor auto-hides after 3000ms; Escape key navigates back to `/party/$partyId`. All 12 lock-now decisions applied. All 9 open questions resolved inline by the orchestrator: OQ-1 `refetchInterval: 5000`, OQ-2 pure CSS Ken Burns, OQ-3 View Transitions API via style passthrough, OQ-4 TvTrackCard at `entities/track/ui/` (cross-spec lock), OQ-5 `bg-black/60` inside TvTrackCard, OQ-6 `party.settings?.tvDisplayText` (field already typed), OQ-7 `metadata.backgroundImages` (field already typed), OQ-8 Escape navigates back, OQ-9 public route (no auth). HeroUI primitives via MCP: Spinner (loading/error/empty panels). Chip was queried but its color tokens don't include a semi-transparent white variant; the join code pill is hand-rolled per the HeroUI-defaults rule. Placeholder query hook at `pages/party-tv/api/use-tv-queue-query.ts` returns `{ tracks: [], metadataByKey: {} }` until the backend lands; reuses `usePartyQuery` + `usePlaybackQuery` from `pages/party/api/` (same-layer cross-slice import). 33 new tests (31 page tests covering all 26 spec section 12 cases + 5 extras + 1 PartyPage TV short-circuit test + 1 TvTrackCard style passthrough). Reviewer-driven fixes applied before commit: (HIGH) extended TvTrackCard with `style` prop so `viewTransitionName` lands on the `<li>` directly, eliminating the wrapper `<div>` that had broken `<ul>`/`<li>` HTML validity + ARIA list announcement; (MINOR) threaded `playback` from `usePlaybackQuery` through PopulatedLayout to PlaybackProgressBar instead of `void playback` + hardcoded `null`; (MINOR) added a PartyPage test verifying the TV short-circuit returns just `<Outlet />` with no QueueDrawer or header rendered. CrowdTune-original copy items marked "copy review" inline: "Party unavailable" (error panel), "Queue is empty" (empty queue heading), "Now playing" (region aria-label), and the aria-live announcement template "Now playing: [track] by [artist]".

## Cloud Functions (`.reference/festify/functions/`)

- `[i]` `functions/lib/spotify-auth.ts` - Spotify OAuth handshake + refresh token storage. Phase 9 backend wedge. Spec at `docs/specs/spotify-auth.spec.md` passed firewall review. Locked: per-user credentials row, server-side encrypted refresh tokens, AES-GCM with fresh nonce per write, key from `SPOTIFY_TOKEN_ENC_KEY` env var. Slice plan:
  - **9a (DONE)**: `infra/migrations/20260510191527_add_spotify_credentials.sql` creates the `spotify_credentials` table with 9 columns, UNIQUE on `user_id`, FK to `neon_auth."user"(id) ON DELETE CASCADE`. Migration not yet applied to the live database.
  - **9b.1 (DONE)**: `apps/api/internal/tokencrypto/` AES-256-GCM package with `Encrypt`, `Decrypt`, `KeyFromHex`. 22 tests passing with race detector. Security-reviewer cleared all 8 threat-model categories (AEAD config, nonce handling, error opacity, buffer safety, key validation, CSPRNG, test coverage, scope).
  - **9b.2 (DONE)**: `apps/api/internal/spotify/` package (HTTP client + `ExchangeCode` + internal `RefreshAccessToken` helper; retries once on 5xx with 500ms fixed pause; 4xx not retried; sentinel errors `ErrSpotifyBadRequest` / `ErrSpotifyUnavailable`) + Handler A (`POST /api/spotify/token`, JWT-gated under `/api`) + sqlc-generated queries from `apps/api/internal/db/queries/spotify_credentials.sql` (Upsert, GetByUserID, DeleteByUserID) + OpenAPI entry with `bearerAuth` + 4 Spotify env vars wired into `config.go` and graceful-degraded in `main.go` (`spotify_enabled=false` log when any var missing). 19 new tests (14 spotify package + 5 handler), all passing with `-race`. Spec-fidelity + security review: 12/12 PASS on the threat model (no token values in logs or responses, encrypt-before-write enforced, JWT auth at group level, parameterized SQL via sqlc, retry policy verified). Go reviewer cleared after two minor nits applied (`errors.Is` instead of `==` for sentinel comparison; lifted `spotifyTokenBody` calls out of handler closures for goroutine safety).
  - **9c (final)**: `/security-review` of the whole + a Spotify-stubbed integration test before merge.
  - Handler C (catalog client-credential token) deferred until search is needed. Handler D (account linking) likely fully replaced by Better Auth's native OAuth provider linking - evaluate before writing any code.
- `[i]` `functions/lib/vote-processor.ts` - vote ranking + queue mutation backend. Phase 10 backend wedge. Spec at `docs/specs/vote-processor.spec.md` passed firewall review (zero code fences, ~3,100 words across 9 sections). Spec section 8 names three required tables (`parties`, `queue_tracks`, `user_votes`); all 8 open questions plus extra schema decisions locked by the orchestrator below. Slice plan:
  - **10a (DONE `f90a370`)**: spec written + firewall verified + open questions resolved.
  - **10b.1 (DONE `d44430a`)**: single goose migration `add_parties_queue_tracks_user_votes.sql` creates all three tables in dependency order (parties first, then queue_tracks, then user_votes). Migration written but not yet applied to the live database.
  - **10b.2.A (DONE `d43fd7e`)**: sqlc queries + Gin handler for `POST /api/parties` + `GET /api/parties/{partyId}` + 6-char short-code generator with 5-retry-on-23505 collision loop. sqlc.yaml gained uuid->string overrides for `parties.host_user_id` and `user_votes.user_id`. 11 new handler tests.
  - **10b.2.B (DONE `a420c39`)**: sqlc query + Gin handler for `GET /api/parties/{partyId}/tracks`. Returns the queue ordered by `(order_idx ASC, added_at ASC)`. 6 new tests.
  - **10b.2.C (DONE `132e7cb`)**: sqlc queries + Gin handler for `PUT/DELETE /api/parties/{partyId}/tracks/{provider}/{trackId}/vote`. Implements all four spec cases inside a single transaction (READ COMMITTED + `SELECT ... FOR UPDATE` on the affected `queue_tracks` row). VoteStorer + VoteOps interface split keeps tests in-memory. 15 new tests covering each algorithm branch.
  - **10c (DONE)**: `/security-review` ran on the full Phase 10 stack. The reviewer flagged one CRITICAL (auth bypass: `auth.RequireUser` was per-route on `/me` only; the `/api` group had no `Use()` call so every other route was unauthenticated). Fixed at `5fda177` by mounting `api.Use(auth.RequireUser(...))` at the group level in `cmd/api/main.go` and simplifying `RegisterMe` to drop the per-route attachment. The fix retroactively closes the same bypass on Phase 9's `POST /api/spotify/token`. 84 Go tests pass with `-race`.

  **Phase 10 follow-ups:**
  - DONE `3486dc2`: `http.MaxBytesReader` body cap (64 KiB) + `name` length validation (200 chars) on `POST /api/parties`. Pathological payloads are now rejected at the `c.ShouldBindJSON` boundary; overlong names return 400 `invalid_field`.
  - DONE `086376e`: TOCTOU race on the vote transaction closed by the partial unique index `CREATE UNIQUE INDEX idx_one_playing_per_party ON queue_tracks (party_id) WHERE order_idx = -9007199254740990` (migration `20260512131018`) plus a bounded retry loop (`maxVoteTxRetries = 3`) around the InTx call, narrowly scoped to `isUniqueViolation(err)`. The InTx body was extracted into `votesHandler.attemptVote` for clarity. 3 new tests cover retry-once, retries-exhaust, and non-23505-does-not-retry.
  - OPEN: rate limiting on the vote endpoints and `POST /api/spotify/token` (project-wide gap; both endpoints share this exposure). Defense-in-depth follow-up, not a blocker.
  - DONE `0be65f0`: black-box wiring test at `apps/api/internal/handlers/wiring_test.go` mirrors the production `cmd/api/main.go` auth wiring and asserts 401 `missing_bearer` on every protected endpoint (`/api/me`, `POST /api/parties`, `GET /api/parties/:id`, `GET /api/parties/:id/tracks`, `PUT/DELETE` vote, `POST /api/spotify/token`). Regression guard for the bypass closed in `5fda177`.
  - OPEN: apply the migrations (`20260512113057` + `20260512131018`) to the live database. Both are non-destructive additions; defer to the next deployment window.

  **Lock-now decisions (orchestrator, before implementer dispatch):**
  - **Q1 + Q7 (concurrency model)**: synchronous in-handler ranking inside a single Postgres BEGIN/COMMIT at READ COMMITTED isolation. Lock the affected `queue_tracks` row with `SELECT ... FOR UPDATE` before reading topmost and computing new order. Re-read topmost track inside the same transaction (Q7 lock: yes). New-track races (when no row exists yet to lock) are serialized by the `user_votes` PK uniqueness. No application-level retry loop because pessimistic locking blocks. No Postgres NOTIFY / no background worker / no LISTEN - the handler is the trigger.
  - **Q2 (trigger architecture)**: same as Q1 - synchronous handler. Rejected: NOTIFY+LISTEN, polling, separate worker. Reason: low concurrency expected (dozens of voters per party), immediate vote-response consistency is a UX win, no infrastructure to maintain.
  - **Q3 (`VOTE_FACTOR` constant)**: store `added_at` and `parties.created_at` as `timestamptz` (matches spotify_credentials convention). Compute the ranking formula in Go: `time_since_ms = added_at.Sub(party.CreatedAt).Milliseconds()`; `order_idx = time_since_ms - vote_count * voteFactor` with `const voteFactor int64 = 1_000_000_000_000`. The `order_idx` column is `bigint`.
  - **Q4 (sort sentinel)**: `order_idx` is `bigint`. Playing-track sentinel is `const playingSentinel int64 = -9_007_199_254_740_990` (`Number.MIN_SAFE_INTEGER + 1`). Kept JS-compatible for cross-stack debugging.
  - **Q5 (party LRU cache)**: DEFERRED. No in-process cache; rely on the per-party PK lookup which is a single indexed fetch. Revisit if profiling shows the party-fetch on the vote path is a bottleneck.
  - **Q6 (loose equality)**: use strict equality (sqlc types are strict). The no-op optimization (skip UPDATE when new `order_idx` equals existing) still applies.
  - **Q8 (authorization)**: JWT required, NO party-membership table, NO anonymous voting. Any authenticated user can vote on any party they have the short-code URL for (matches Festify's URL-as-membership model). The `allowAnonymousVoting` PartySettings knob is a future enhancement (guest sessions); not part of this port. Vote uniqueness enforced by `user_votes` PK on `(party_id, provider, provider_track_id, user_id)`.

  **Schema lock (orchestrator, beyond the 8 OQs):**
  - `parties.id` is the URL-facing short code (TEXT, 6-char alphanumeric, server-generated at create time with collision-retry). No separate `short_id` column. API response carries both `id` and `shortId` with the same value (future frontend cleanup to drop `shortId` from the Party type).
  - `parties.host_user_id` is uuid FK to `neon_auth."user"(id)` ON DELETE CASCADE.
  - `parties.settings` is `jsonb NOT NULL DEFAULT '{}'::jsonb` for PartySettings (allowAnonymousVoting, tvDisplayText, etc.).
  - `parties.is_active` is `bool NOT NULL DEFAULT true` to support ended-but-not-deleted parties.
  - `queue_tracks` composite PK on `(party_id, provider, provider_track_id)`. Reserved word `order` renamed to `order_idx`. NO `is_playing` column - the playing track is identified by `order_idx = playingSentinel` per spec section 5 invariant.
  - `queue_tracks.vote_count` integer with `CHECK (vote_count >= 0)`. Handler maintains the invariant by deleting (Case 4) rather than allowing negatives.
  - Index `queue_tracks (party_id, order_idx ASC)` serves both the topmost-track lookup and the full-queue read.
  - `user_votes` PK on `(party_id, provider, provider_track_id, user_id)`. NO FK to `queue_tracks` (a vote may create the `queue_tracks` row; FK ordering conflict). FK on `party_id` -> `parties(id)` ON DELETE CASCADE; FK on `user_id` -> `neon_auth."user"(id)` ON DELETE CASCADE. Application handler enforces consistency between `user_votes` and `queue_tracks`.
  - Index `user_votes (party_id, user_id)` for "what has this user voted on in this party?" lookups (used by PartyTrackRow's `hasVoted` derivation in the frontend).

  **Endpoint lock (orchestrator, 5 endpoints for Phase 10b.2):**
  - `POST /api/parties` - create. Body: `{ name, settings? }`. Host = JWT `sub`. Returns the new party.
  - `GET /api/parties/:partyId` - read party metadata. Auth: JWT required.
  - `PUT /api/parties/:partyId/tracks/:provider/:trackId/vote` - cast vote (idempotent). Creates `queue_tracks` row if absent (Case 2). Triggers ranking inside the same transaction.
  - `DELETE /api/parties/:partyId/tracks/:provider/:trackId/vote` - retract vote (idempotent). Deletes `queue_tracks` row when `vote_count` drops to 0 and the track is not playing and not a fallback (Case 4).
  - `GET /api/parties/:partyId/tracks` - fetch ordered queue. Auth: JWT required. Returns array sorted by `order_idx ASC`.
  - All five carry `security: [bearerAuth: []]` in `openapi.yaml`.

  **Translator overrides (orchestrator):**
  - Spec section 9 proposed `POST /api/parties/{partyId}/tracks` for "add a track without a vote" - REJECTED. Adding == voting in CrowdTune (matches Festify behavior + simplifies the handler surface). The PUT vote endpoint creates the `queue_tracks` row on first vote (spec Case 2).
  - Spec Q8's "anonymous voting" decisively deferred (see above) - not part of this port.

## Notes

- We do **not** plan to port Festify's Polymer custom-element tooling, Polymer iconset, or its specific
  Firebase RTDB schema layout. Those are implementation details we redesign for our React + Postgres stack.
- We **do** plan to preserve Festify's domain model: parties identified by short codes, fallback playlists,
  upvote-only voting (no downvotes), stable-sort with vote counts, anonymous guest joining.
